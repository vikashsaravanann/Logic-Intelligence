/**
 * Stub re-export of COMPANY config for use in email templates.
 * Email templates live outside the Next.js src/ tree and cannot use @/ aliases,
 * so this file re-exports the config with a relative path.
 */
export { COMPANY } from '../src/config/company';
