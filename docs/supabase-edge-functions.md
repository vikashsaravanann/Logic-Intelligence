# Supabase Edge Functions

**Status:** NOT REQUIRED / NOT IMPLEMENTED

This project relies on Vercel Node.js Serverless Functions for all backend logic, particularly Nodemailer/Gmail SMTP (which is incompatible with Supabase's Deno Edge Functions). 

If an Instagram Graph API sync or cron job is required in the future, it can be deployed here. Currently, no Edge Functions are required.
